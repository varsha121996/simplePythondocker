name: CI/CD for Python Docker App

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run tests (if any)
        run: |
          echo "Add your test commands here, e.g., pytest"
          # pytest tests/

    #  - name: Build Docker image
    #    run: |
     #     docker build -t simple-python-app .

      # Optional: Push to Docker Hub or GHCR
      # - name: Log in to Docker Hub
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}

      # - name: Push Docker image
      #   run: |
      #     docker tag simple-python-app your-dockerhub-username/simple-python-app:latest
      #     docker push your-dockerhub-username/simple-python-app:latest
